```shell
git worktree add ../ssr-demo-csr csr/react
git worktree list

git worktree remove ../ssr-demo-csr
git worktree prune
```

```shell
..
├── ssr-demo/          # ssr/express branch
└── ssr-demo-csr/      # csr/react branch
```
